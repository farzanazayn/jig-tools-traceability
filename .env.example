# Copy this file to .env (do NOT commit .env — it's gitignored) and fill in
# the real values. .env is read automatically by the backend (python-dotenv)
# and by docker-compose (env_file).

DATABASE_URL=postgresql://<user>:<password>@<host>:5432/<database>
DB_SCHEMA=jigtools
